server:
  port: 8082
  
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/VaccinationCentreDB
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  application:
    name: VACCINATION-CENTRE-SERVICE
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect